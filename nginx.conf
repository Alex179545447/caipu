# Nginx 配置文件模板
# 保存为: /etc/nginx/conf.d/caipu.zhimai-ai.cn.conf

server {
    listen 80;
    server_name caipu.zhimai-ai.cn;

    # 网站根目录
    root /www/caipu.zhimai-ai.cn;
    index index.html;

    # 字符集
    charset utf-8;

    # Gzip压缩
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml application/json application/javascript application/rss+xml application/atom+xml image/svg+xml;

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, no-transform";
        access_log off;
    }

    # HTML 不缓存（确保更新及时生效）
    location ~* \.html$ {
        expires -1;
        add_header Cache-Control "no-store, no-cache, must-revalidate";
    }

    # SPA 路由支持（重要！）
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 安全 headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # 访问日志
    access_log /var/log/nginx/caipu_access.log;
    error_log /var/log/nginx/caipu_error.log;

    # 最大上传大小
    client_max_body_size 10M;
}

# HTTPS 配置（申请SSL证书后取消注释）
# server {
#     listen 443 ssl http2;
#     server_name caipu.zhimai-ai.cn;
#
#     ssl_certificate /etc/letsencrypt/live/caipu.zhimai-ai.cn/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/caipu.zhimai-ai.cn/privkey.pem;
#     ssl_trusted_certificate /etc/letsencrypt/live/caipu.zhimai-ai.cn/chain.pem;
#
#     # SSL 配置
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
#     ssl_prefer_server_ciphers off;
#
#     # HSTS
#     add_header Strict-Transport-Security "max-age=31536000" always;
#
#     # 其他配置与HTTP相同...
# }
